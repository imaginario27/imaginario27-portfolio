export enum GalleryLayout {
    GRID = 'grid',
    MASONRY = 'masonry',
}

export enum GalleryPaginationMode {
    NONE = 'none',
    PAGINATION = 'pagination',
    LOAD_MORE = 'load-more',
    INFINITE = 'infinite',
}

export enum GalleryWidowAlign {
    LEFT = 'left',
    CENTER = 'center',
    JUSTIFY = 'justify',
    HIDE_IF_SINGLE = 'hide-if-single',
}

export enum GallerySortBy {
    NONE = 'none',
    DATE_DESC = 'date-desc',
    DATE_ASC = 'date-asc',
    TITLE_ASC = 'title-asc',
    TITLE_DESC = 'title-desc',
    RANDOM = 'random',
}

export enum GalleryCaptionPlacement {
    HOVER = 'hover',
    OVERLAY_BOTTOM = 'overlay-bottom',
    BELOW = 'below',
    NONE = 'none',
}
